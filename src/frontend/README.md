# Frontend

This directory is reserved for the Next.js App Router frontend.

Initialization is required using `create-next-app@latest` per requirements. If you see an empty folder, run:

```
cd src
npx create-next-app@latest frontend --ts --tailwind --eslint --app --src-dir --import-alias "@/*"
```

After initialization, record `next --version` in the root README.
